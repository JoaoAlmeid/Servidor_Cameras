import path from 'path';

const __dirname = process.cwd();

export const caminho_camera = path.join(__dirname, 'transmissoes');
export const caminho_arquivos = path.join(__dirname, '../uploads');
